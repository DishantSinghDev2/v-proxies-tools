# Generate with: npm run generate-keys
ECDH_PRIVATE_KEY_JWK={"kty":"EC","crv":"P-256",...}
ECDH_PUBLIC_KEY_B64=<base64-encoded-raw-public-key>

# Random secret — share with Cloudflare Worker via wrangler secret
BACKEND_TOKEN=your-random-secret-token-here

# Server config
PORT=3001
